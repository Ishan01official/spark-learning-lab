# Tests

This repo is primarily a learning lab, so validation currently focuses on:

- repository structure
- required README files
- empty markdown files
- Python syntax
- internal markdown links

Run:

```bash
make validate
```

As more reusable project code is added, add focused unit tests here.
